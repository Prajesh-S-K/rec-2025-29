Start
Step 1 : INPUT : ch # input stage
Step 2 : IF ch == 'A' OR ch == 'E' OR ch == 'I' OR ch == 'O' OR ch == 'U' OR
            ch == 'a' OR ch == 'e' OR ch == 'i' OR ch == 'o' OR ch == 'u',
         then goto Step 3 Otherwise goto Step 4 # process stage 1
Step 3 : OUTPUT : "The character is a vowel" # output stage 1
Step 4 : OUTPUT : "The character is a consonant" # output stage 2
Step 5 : End
